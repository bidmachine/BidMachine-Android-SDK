apply plugin: 'com.android.library'

android {
    defaultConfig {
        versionCode 6
    }
}

apply from: '../adapters.gradle'

dependencies {
    adapterDependency 'com.facebook.android:audience-network-sdk:6.3.0'
}