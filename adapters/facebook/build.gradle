apply plugin: 'com.android.library'

android {
    defaultConfig {
        versionCode 4
    }
}

apply from: '../adapters.gradle'

dependencies {
    adapterDependency 'com.facebook.android:audience-network-sdk:6.0.0'
}